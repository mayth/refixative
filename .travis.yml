language: ruby
rvm:
  - 2.1.0
before_script:
  - psql -c 'create database refixative2_test;' -U postgres
  - bin/rake db:migrate
  - bin/rake db:seed
script: bundle exec rake spec
branches:
  only:
    - next
