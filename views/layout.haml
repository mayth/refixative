!!! 5
%html
  %head
    - if @page_title && !@page_title.empty?
      %title== #{@page_title} - Refixative
    - else
      %title Refixative
    %link{rel: 'stylesheet', type: 'text/css', href:'/style.css'}
    %script{src: '/script/jquery.js'}
  %body
    #top_container
      != yield
    #footer
      %p
        query at
        = Time.now.getutc.strftime('%F %T')
        (UTC)
      %p
        Refixative -- REFLEC BEAT colette score tool
      %p== rev. #{settings.revision}
      %p
        <a href="https://twitter.com/maytheplic">@maytheplic</a> - May Akizuru

